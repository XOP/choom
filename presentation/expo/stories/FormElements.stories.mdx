import { Meta } from "@storybook/addon-docs";

import Select from "choom/src/components/select/Select";
import SelectOption from "choom/src/components/select/SelectOption";
import Switch from "choom/src/components/switch/Switch";
import Numeric from "choom/src/components/numeric/Numeric";

<Meta title="Choom Form Elements" />

<style>{`
  section.s {
    padding: 2rem;
    margin: 1rem 0 2rem;
    border: 1px solid #ccc;
    border-radius: 0.25rem;
  }

  h2 { 
    border: none !important;
   }
`}</style>

# Choom common

## Select

<section className="s">
  <Select size="small" value="two" onChange={(val) => console.log(val)}>
    <SelectOption value="one">Option One</SelectOption>
    <SelectOption value="two">Option Two</SelectOption>
    <SelectOption value="three" disabled>Another Option</SelectOption>
  </Select>

  <br />

  <Select size="regular" value="two" onChange={(val) => console.log(val)}>
    <SelectOption value="one">Option One</SelectOption>
    <SelectOption value="two">Option Two</SelectOption>
    <SelectOption value="three" disabled>Another Option</SelectOption>
  </Select>

  <br />

  <Select size="big" value="two" onChange={(val) => console.log(val)}>
    <SelectOption value="one">Option One</SelectOption>
    <SelectOption value="two">Option Two</SelectOption>
    <SelectOption value="three" disabled>Another Option</SelectOption>
  </Select>
</section>

<section className="s">
  <Select value="two" disabled>
    <SelectOption value="one">Option One</SelectOption>
    <SelectOption value="two">Option Two</SelectOption>
    <SelectOption value="three" disabled>Another Option</SelectOption>
  </Select>
</section>


## Switch

<section className="s">
  <p>
    from this <Switch /> to that
  </p>
  <p>
    where <Switch checked /> that is checked
  </p>
  <p>
    and this <Switch disabled /> is disabled
  </p>
</section>


## Numeric

<section className="s">
  <Numeric value={42} />
</section>

<section className="s">
  <Numeric value={42} disabled />
</section>
