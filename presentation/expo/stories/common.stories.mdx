import { Meta } from '@storybook/addon-docs';

import Button from "choom/src/components/button/Button";
import Card from "choom/src/components/card/Card";
import Chip from "choom/src/components/chip/Chip";
import Divider from "choom/src/components/divider/Divider";
import Icon from "choom/src/components/icon/Icon";
import Loader from "choom/src/components/loader/Loader";

import { Clock } from 'react-feather';

<Meta title="Choom Common" />

<style>{`
  section.s {
    padding: 2rem;
    margin: 2rem 0;
    border: 1px solid #ccc;
    border-radius: 0.25rem;
  }
`}</style>

# Choom common

<section className="s">
  <Button size='big'>Button</Button>
  <Button>Button</Button>
  <Button size='small'>Button</Button>
</section>

<section className="s">
  <Button fluid>Button</Button>
</section>

<section className="s">
  <Icon size='small'>
    <Clock />
  </Icon>

  <Icon>
    <Clock />
  </Icon>

  <Icon size='big'>
    <Clock />
  </Icon>
</section>

<section className="s">
  <Loader  />
  <Divider mt='2' mb='2' />
  <Loader size='small' display='inline-block' />
  <Loader size='regular' display='inline-block'/>
  <Loader size='big' display='inline-block' />
</section>

<section className="s">
  <Chip>Chip</Chip>
  <Chip onClick={() => console.log('Chip onClick')}>Chip with onClick</Chip>
  <Chip>Chip with a taaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaail</Chip>
</section>

<section className="s">
  <Card>Card with Divider at the bottom</Card>
  <Divider mt='2' mb='2' />
  <Card>Card with Divider at the top</Card>
</section>
